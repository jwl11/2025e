# 《2026电赛控制类项目工程分工与准备计划》

> 仓库：`https://github.com/jwl11/2025e`  
> 审计分支：`main`（审计时 HEAD：`a0137fd`）  
> 审计日期：2026-07-23  
> 冲刺周期：2026-07-23 ～ 2026-07-28；2026-07-29 进入比赛换题模式  
> 目标平台：TI MSPM0G3507 + MaixCAM2（仓库内名称为“视觉”）

## 使用说明与证据边界

本文只把仓库中能直接看到的源码、SysConfig、构建产物当作“已实现证据”。没有原理图、BOM、接线表、示波器记录、烧录记录或实车视频的项目，统一标为“缺失”或“待实物验证”，不能因为存在目录名、注释或历史 `.out` 文件就判定完成。

当前最重要的结论：仓库不是一个可直接参赛的控制工程，而是“TI 空工程骨架 + 两路 PWM/UART0 配置 + MaixCAM2 矩形视觉代码”。未来六天不应平均铺开所有功能，必须先打通“编码器 → 速度环 → 电机”和“视觉 → UART → MSPM0 → 状态机”两条最小闭环。

---

# 第一部分 项目现状分析

## 1. 硬件平台

| 项目 | 仓库证据 | 当前结论 | 立即动作 |
|---|---|---|---|
| MSPM0 型号 | `empty.syscfg` 指定 `MSPM0G3507`、LQFP-64；目标配置为 MSPM0G3507 | **MSPM0G3507 已确定** | 锁定比赛板型号和引脚表，不再临时换 MCU |
| 内核/工具链 | Cortex-M0+、TI Clang 4.0.4 LTS、MSPM0 SDK 2.10.00.04 | 工具链工程已建立 | 记录 CCS/SysConfig 版本，三台电脑统一 |
| 电机驱动方式 | `empty.syscfg` 配置 TIMA0 两路边沿对齐 PWM：PA0/CCP0、PA1/CCP1，周期 1000，时钟 32 MHz | 仅有约 32 kHz 的两路 PWM 输出配置；**方向、使能、刹车、故障脚未实现** | 根据实物驱动板补 DIR/EN/FAULT 和安全停机 |
| 电机驱动型号 | 仓库无 BOM、原理图、宏定义或说明 | **【缺失，需要补充】** | Day1 填写型号、电压、峰值电流、逻辑电平、真值表 |
| 电机数量 | 两路 PWM 不能证明电机数量，也不能证明双路 H 桥 | **【缺失，需要补充】** | 默认按两轮差速车规划，实物若非两电机立即修订 |
| 编码器接口 | SysConfig 无 QEI/捕获/GPIO 中断配置，驱动目录为空 | **【缺失，需要补充】** | 两电机各准备 A/B 相；优先硬件正交编码器或定时器捕获 |
| 摄像头接口 | MaixCAM2 代码独立运行；MSPM0 已配置 UART0：PA10 TX、PA11 RX、115200 8N1 | 物理接口意图应为 UART，但视觉代码尚未发送数据 | 明确共地与 3.3 V 电平；示波器确认数据帧 |
| IMU 接口/型号 | 无 I2C/SPI 配置、无 IMU 驱动 | **【缺失，需要补充】** | Day1 确定型号；优先 I2C，保留 SPI 作为高频备选 |
| 灰度/循迹传感器 | 无 ADC/GPIO 阵列配置 | **【缺失，需要补充】** | 确定模拟/数字输出和通道数，再分配 ADC/GPIO |
| 其他传感器 | 无测距、限位、霍尔、电流/电压采样代码 | **【缺失，需要补充】** | 只按赛题高概率准备接口，不在 Day1 盲目堆模块 |
| 通信接口 | UART0 已配置但无收发驱动、ISR、DMA、协议解析 | 外设初始化存在，通信功能未完成 | Day1 完成环回；Day2 完成定长帧 + CRC + 超时 |
| I2C/SPI/ADC/DMA | SysConfig 均未配置 | **【缺失，需要补充】** | 按已选 IMU、传感器和吞吐需求配置 |
| 保护与可靠性 | 无看门狗、欠压检测、过流/FAULT 处理、急停逻辑 | **【缺失，需要补充】** | 默认上电 PWM=0；通信超时和故障输入强制停车 |

### Day1 必须补齐的硬件参数表

| 参数 | 当前值 |
|---|---|
| 电机型号、额定电压、减速比 | 【缺失，需要补充】 |
| 驱动板型号、PWM/DIR/EN/FAULT 真值表 | 【缺失，需要补充】 |
| 编码器线数/PPR、A/B 相电平 | 【缺失，需要补充】 |
| 轮径、轮距 | 【缺失，需要补充】 |
| IMU 型号、接口、电平、安装方向 | 【缺失，需要补充】 |
| 灰度传感器数量、模拟/数字接口 | 【缺失，需要补充】 |
| 电池节数、电压范围、DC-DC 额定/峰值电流 | 【缺失，需要补充】 |
| MaixCAM2 供电电压、峰值电流、UART 引脚 | 【缺失，需要补充】 |

## 2. 软件架构分析

### 2.1 当前目录和实际构建关系

```text
2025e/
├── empty.c                         # 当前唯一业务入口；初始化后空循环
├── empty.syscfg                    # 两路 PWM + UART0
├── MSPM0G3507/
│   ├── Application/               # app.c/app.h 为空；app_motor.c 仅注释
│   ├── BSP/                       # bsp_motor.c/.h 为空
│   ├── Driver/                    # drv_tim、drv_uart 均为空
│   └── Middleware/                # mid_pid.c/.h 为空
├── 视觉/
│   ├── main.py                    # OpenCV 高对比度四边形检测
│   ├── main_ai.py                 # YOLO11 黑框检测 + 短时预测
│   ├── AI抗遮挡/                  # 可直接作为 MaixVision 工程同步
│   └── model/                     # .mud/.axmodel 模型
└── Debug/                         # 已提交的历史构建产物
```

当前 `Debug/subdir_vars.mk` 只编译 `empty.c`、SysConfig 生成文件和启动文件；`MSPM0G3507` 下的业务文件没有进入现有构建。即使以后把代码写入这些空文件，也必须先确认 CCS 已将目录纳入构建。

### 2.2 已完成模块

- TI MSPM0G3507 DriverLib/SysConfig 基础工程可生成历史 `first.out`。
- TIMA0 的 PA0/PA1 两路 PWM 已配置，当前配置推算频率约 32 kHz；尚无业务 API 调占空比。
- UART0 的 PA10/PA11、115200、8N1 已配置；尚无中断接收、发送、缓冲区和协议。
- `视觉/main.py` 已实现灰度、GaussianBlur、Canny、闭运算、轮廓筛选、四边形中心和像素误差计算。
- `视觉/main_ai.py` 与 `视觉/AI抗遮挡/main.py` 已实现 YOLO11 最大目标选择、指数平滑和最多 30 帧短时预测，并随仓库保存模型。
- 视觉侧能输出 `found/valid、cx、cy、ex、ey、area/fps` 等调试信息。

### 2.3 代码质量问题与阻断项

| 优先级 | 问题 | 证据/风险 | 修改建议 |
|---|---|---|---|
| P0 | 业务源文件未进入构建 | `Debug/subdir_vars.mk` 无 Application/BSP/Driver/Middleware | 在 CCS 中纳入目录，Clean/Rebuild 后用 `.map` 确认符号存在 |
| P0 | include 路径拼写错误 | `.cproject` 写成 `MSPM0G35507`，实际目录为 `MSPM0G3507` | 修正所有 4 条 include path，重新生成 makefile |
| P0 | 主循环为空 | `empty.c` 只有 `SYSCFG_DL_init()` 和空 `while(1)` | 改为 `app_init()` + 固定周期 `app_run()`/调度器 |
| P0 | 电机与 PID 只有空文件 | 不能驱动、测速或闭环 | 先实现两轮速度环，再做位置/航向外环 |
| P0 | 视觉没有 UART | `main.py` 注释明确写“后续接 UART” | 新建 `vision_uart.py`，固定频率发送二进制帧 |
| P0 | 没有失联停车 | 视觉数据陈旧仍可能被使用 | MSPM0 记录最后有效帧时间，超过 100 ms 降级/停车 |
| P1 | README 与代码版本不一致 | README 要求 `opencv_v5_edge_track`，代码打印 `opencv_v3` | 统一版本号；删除不存在参数或补实现 |
| P1 | README 描述不存在的传统视觉参数 | `CENTER_SMOOTH_ALPHA`、`LOST_HOLD_FRAMES` 不在 `main.py` | 传统方案补滤波/丢失策略，或修正文档 |
| P1 | AI 主程序重复 | 两处 `main_ai.py/main.py` 内容重复，易产生版本漂移 | 只保留 `视觉/AI抗遮挡` 为可部署工程，根目录放说明/入口 |
| P1 | PWM 名为 `BLDC` 但架构目标是差速车 | 名称可能误导驱动方式；缺 DIR/EN | 按真实驱动器命名 `MOTOR_PWM`，通道命名 LEFT/RIGHT |
| P1 | UART 实例名为 `step` | 无法表达 MaixCAM2 链路用途 | 改名 `VISION_UART`，调试 UART 另开实例 |
| P1 | 生成物进入 Git | `Debug/*.o/*.out/*.map` 等被跟踪 | 添加 `.gitignore`；在版本冻结前清理跟踪策略 |
| P2 | 根 README 仍是 TI Empty 示例 | 新成员无法按仓库启动 | 改成硬件接线、构建、烧录、通信和验收入口 |
| P2 | 无测试/标定数据 | PID、视觉阈值不可复现 | 新建 `docs/calibration/` 保存参数与测试记录 |

### 2.4 需要重构的边界

- 当前 `Middleware` 建议拆成 `Control` 和 `Algorithm`，避免 PID、滤波、协议、业务状态混在一层。
- BSP 只表达板级器件组合，例如电机、编码器、IMU；Driver 只封装片上外设，不写业务状态机。
- ISR 只取数、清中断、写环形缓冲或计数；解析帧、PID 计算、状态迁移都放在固定周期任务中。
- Cortex-M0+ 无硬件浮点单元。1 kHz 速度环优先整数/定点或经过测时的 `float`，禁止在 ISR 中做矩阵运算或打印。
- 不使用无边界动态内存；共享的 ISR 数据使用 `volatile`，多字节快照加临界区或双缓冲。

## 3. 当前项目评分

> 分数只反映仓库完成度，不代表实物完成度。

| 项目 | 分数 | 原因 |
|---|---:|---|
| 硬件完成度 | 2/10 | 仅能确认 MCU、两路 PWM 引脚和一个 UART；驱动型号、电源、编码器、IMU、传感器、保护均无证据 |
| 主控完成度 | 2/10 | SysConfig 能生成基础工程，但 Application/BSP/Driver/PID 为空且未进入构建 |
| 控制算法 | 0.5/10 | 只有 PID 文件名，没有实现、调参记录、速度/位置闭环或运动学 |
| 视觉 | 4/10 | 已有传统矩形与 YOLO11 黑框检测；缺巡线、色块、通用图形、UART 协议和实测指标 |
| 系统联调 | 0/10 | 无视觉收发、无电机闭环、无状态机、无连续运行/断电恢复记录 |

**总评：当前适合做视觉算法试验和 MSPM0 外设起点，不具备参赛闭环。P0 是把“空骨架”变成可编译、可驱动、可通信、可安全停车的最小系统。**

---

# 第二部分 总体工程架构设计

## 1. 最终分层

```text
Application
    │  任务状态、模式切换、赛题流程、故障降级
Control
    │  速度/位置/航向闭环、运动控制器
Algorithm
    │  PID、滤波、里程计、轨迹与数学工具
Driver / Communication / Vision Adapter
    │  片上外设、协议编解码、视觉数据适配
BSP
    │  电机/编码器/IMU/传感器等板级器件
MSPM0G3507 Hardware
```

依赖必须单向向下。算法层不能直接操作寄存器，视觉解析器不能直接写 PWM，Application 只能通过 Control 发运动目标。

## 2. 目标目录 `MSPM0_Project`

比赛前不建议一次性移动 CCS 工程根目录。先在当前工程验证构建，再按以下结构迁移；每移动一层都执行 Clean/Rebuild。最终目录如下：

```text
MSPM0_Project/
├── BSP/
│   ├── bsp_motor.c/.h              # 左/右电机方向、使能、刹车、限幅、安全停机
│   ├── bsp_encoder.c/.h            # 双编码器计数快照、方向、清零、故障检测
│   ├── bsp_imu.c/.h                # IMU 上电、读取、量程与安装方向映射
│   ├── bsp_line_sensor.c/.h        # 灰度阵列采样、归一化、离线/丢线判断
│   ├── bsp_power.c/.h              # 电池/母线采样、欠压与驱动 FAULT
│   └── bsp_board.c/.h              # 安全上电顺序、急停、板级自检
├── Driver/
│   ├── drv_gpio.c/.h               # GPIO 输入输出和中断薄封装
│   ├── drv_uart.c/.h               # UART 中断/DMA、环形缓冲、非阻塞发送
│   ├── drv_i2c.c/.h                # IMU/传感器 I2C，含超时和总线恢复
│   ├── drv_spi.c/.h                # 备用高速传感器接口
│   ├── drv_pwm.c/.h                # 双路 PWM 占空比与同步更新
│   ├── drv_adc.c/.h                # 多通道 ADC + DMA 缓冲
│   ├── drv_dma.c/.h                # UART/ADC DMA 通道管理
│   ├── drv_timer.c/.h              # 1 kHz 控制节拍、微秒时间戳
│   ├── drv_encoder.c/.h            # QEI/捕获/计数器底层
│   └── drv_watchdog.c/.h           # 独立看门狗喂狗与复位原因
├── Control/
│   ├── control_speed.c/.h          # 左右轮速度闭环
│   ├── control_position.c/.h       # 距离外环，输出速度目标
│   ├── control_heading.c/.h        # 航向/角度外环，输出差速目标
│   ├── control_line.c/.h           # 巡线误差到角速度目标
│   └── control_gimbal.c/.h         # 若赛题需要，视觉误差到云台目标
├── Algorithm/
│   ├── pid.c/.h                    # 位置式/增量式 PID、限幅、复位、抗饱和
│   ├── filter.c/.h                 # 滑动平均、一阶低通、互补、可选卡尔曼
│   ├── odometry.c/.h               # 双轮里程计
│   ├── motion.c/.h                 # 差速运动学、定距、定角与速度斜坡
│   └── crc16.c/.h                  # 通信 CRC16-CCITT
├── Vision/
│   ├── vision_data.h               # 视觉观测统一结构体
│   ├── vision_adapter.c/.h         # 不同 msg_id 转成统一误差/目标
│   └── vision_health.c/.h          # 帧龄、置信度、连续丢失、降级状态
├── Communication/
│   ├── protocol.h                  # 帧格式、消息 ID、字节序、缩放定义
│   ├── protocol_encode.c/.h        # MSPM0 回传/调参帧
│   ├── protocol_decode.c/.h        # 状态机解析器、CRC、乱流重同步
│   └── telemetry.c/.h              # 低频调试遥测，禁止阻塞控制环
├── Application/
│   ├── app.c/.h                    # 初始化、固定周期调度、总故障处理
│   ├── app_state_machine.c/.h      # INIT~FINISH 状态机
│   ├── app_config.h                # 引脚之外的比赛参数与功能开关
│   ├── app_self_test.c/.h          # 上电电机/编码器/IMU/UART 自检
│   └── app_task_*.c/.h             # 不同题型，仅组合已有能力
├── Config/
│   ├── board_config.h              # 轮径、轮距、编码器 PPR、方向极性
│   └── control_params.h            # PID、限幅、滤波参数及版本号
└── Tests/
    ├── test_protocol.c             # 好帧、坏 CRC、截断、粘包、超时
    ├── test_pid.c                  # 限幅、积分、复位、符号方向
    └── hil_checklist.md            # 硬件在环验收记录
```

## 3. 从现有文件到目标文件的修改映射

| 当前文件 | 建议修改 |
|---|---|
| `empty.c` | 暂时保留为入口，调用 `app_init()`/`app_run()`；冻结前改名为 `main.c` |
| `empty.syscfg` | 增加 DIR/EN/FAULT、双编码器、1 kHz Timer、I2C、ADC/DMA、WDT；`BLDC`/`step` 改为语义名称 |
| `.cproject` | 修正 `MSPM0G35507` 拼写，并确保所有业务目录参与构建 |
| `MSPM0G3507/Application/app_motor.c` | 拆到 `BSP/bsp_motor` 与 `Control/control_speed`，不保留混层控制 |
| `MSPM0G3507/Driver/drv_tim.*` | 改名/扩展为 `drv_timer.*`，只提供节拍和时间戳 |
| `MSPM0G3507/Driver/drv_uart.*` | 实现中断环形缓冲、非阻塞发送和统计计数 |
| `MSPM0G3507/Middleware/mid_pid.*` | 迁移为 `Algorithm/pid.*`，增加输出/积分限幅与抗饱和 |
| `视觉/main.py` | 保留传统矩形检测，补 UART、平滑/丢失策略、版本号一致性 |
| `视觉/main_ai.py` | 与 `视觉/AI抗遮挡/main.py` 合并单一真源，避免重复维护 |
| `README.md` | 替换 TI Empty 说明，增加接线、构建、烧录、启动、协议和验收入口 |

---

# 第三部分 三人团队分工

## 成员 A：硬件负责人

### 职责

- 电池、保险/保护、DC-DC、电源树和测量点。
- 电机、驱动板、编码器、PCB/转接板、接插件和线束。
- MaixCAM2 独立稳压供电与 UART 电平/共地。
- IMU、灰度/测距等传感器接线、安装方向和机械固定。
- 强弱电分区、星形回流、去耦、屏蔽、续流和抗干扰。
- 每次更换硬件后更新 `docs/hardware_wiring.md`，禁止只口头通知。

### 六天任务与验收

| 日期 | 任务 | 当日验收标准 |
|---|---|---|
| Day1 7/23 | 完成 BOM、引脚/接线表、电源树；确认驱动型号、电机数、编码器 PPR、IMU 型号；搭建保险与急停 | 断电测无短路；主控/视觉/电机电源分别可测；所有未知项清零；上电默认 PWM=0 |
| Day2 7/24 | 完成双电机、编码器、IMU、MaixCAM2 线束；布置测试点；检查共地 | 抬轮测试左右轮正反转正确；编码器手转计数方向正确；UART 波形为 3.3 V；无接触不良 |
| Day3 7/25 | 做电机启停、堵转瞬态、电磁铁/舵机等干扰源测试；补 TVS/续流/去耦 | 电机 0→目标转速时 MCU 不复位；驱动不过热；示波器保存 5 V/3.3 V 波形；电源纹波达成团队阈值 |
| Day4 7/26 | 固定结构、质心、电池、相机视角和 IMU 方向；整理线束 | 快速拆装后接线不变；相机/IMU 固定无松动；满转向不拉扯线束；连续跑 30 min 无掉电 |
| Day5 7/27 | 模拟比赛环境，准备备用驱动、DC-DC、线材、电机和工具 | 主车完成全流程；替换一个关键模块后 15 min 内恢复；备件有标签和接线图 |
| Day6 7/28 | 冻结硬件；只处理 P0 安全问题；复核比赛箱 | 连续 2 h 无复位；10 次断电重启正常；运输振动后自检通过；硬件版本贴纸完成 |

### 硬件专项判据

- **电机启动是否复位**：同时采集 3.3 V、5 V、电池母线和 nRESET；做至少 20 次冷启动及正反转阶跃。任何一次复位均判失败。
- **电磁铁/继电器/舵机是否干扰 MCU**：感性负载必须有续流/TVS；单独电源回路优先；动作 100 次，无串口乱码、复位或传感器锁死。
- **电源纹波**：探头使用短地弹簧，在 MCU 和 MaixCAM2 供电端测量；团队需按器件容限设定阈值并留 30% 余量，不能只看万用表。
- **接地**：功率地回流不得穿过 MCU/IMU 小信号地；MaixCAM2 与 MSPM0 必须共地；编码器和 UART 线远离电机相线。

## 成员 B：主控负责人（MSPM0G3507）

### 底层驱动交付

| 模块 | 交付要求 | 验收 |
|---|---|---|
| GPIO | DIR/EN/FAULT/急停/LED，统一有效电平 | 上电方向确定、EN 默认关闭、FAULT 可触发停车 |
| UART | RX 中断或 DMA + 环形缓冲；TX 非阻塞；错误计数 | 连续 10 万帧无解析死锁；坏 CRC 被丢弃 |
| I2C | 超时、NACK、总线恢复 | 拔插/干扰后不永久阻塞主循环 |
| SPI | 预留给高速 IMU/传感器，未使用时不强行启用 | 真实器件到位后再验收 |
| PWM | 双路同步更新、0～最大占空限制、安全置零 | 0/25/50/75% 波形频率、极性正确 |
| ADC | 定时触发 + DMA，多通道平均 | 采样周期稳定，无控制环阻塞 |
| DMA | 只服务明确吞吐瓶颈；通道表留档 | 无通道冲突，完成/错误中断可观测 |
| Timer | 1 kHz 基准节拍，必要时另设 100 Hz 外环 | 示波器/逻辑分析仪测周期抖动 |
| Encoder | 双轮计数、方向、溢出处理、原子快照 | 10 圈人工计数误差为 0，左右方向统一 |
| Watchdog | 只在所有关键任务健康时喂狗 | 人为卡死可复位，复位原因可记录 |

### 1. 电机速度闭环

```text
编码器计数差分
      ↓  固定采样周期、单位换算、低通
实际轮速 rpm/mm·s⁻¹
      ↓
速度 PID（左右轮各一套）
      ↓  输出限幅、积分抗饱和、死区补偿
PWM + DIR
      ↓
电机
```

实现约束：速度环建议 500 Hz～1 kHz；左右轮必须分别标定；目标速度经斜坡限制后再进入 PID；停车时根据场景选择滑行/刹车，并清积分。验收用阶跃曲线而不是“看起来能跑”：记录上升时间、超调、稳态误差和左右轮一致性。

### 2. 位置闭环

```text
目标位置/距离
      ↓
位置 PID（50～100 Hz）
      ↓  输出限幅为允许速度
速度目标
      ↓
左右速度环（500 Hz～1 kHz）
      ↓
电机
```

位置外环不直接写 PWM。定距离同时加入航向修正，避免两轮差异造成跑偏。到位判据必须同时满足“位置误差小”和“速度接近零”并持续若干周期。

### 3. IMU 算法

若 IMU 没有内部融合 MCU，主控需要完成：

- 静止零偏校准，保存均值、方差和温漂测试结果。
- 原始数据坐标映射、量程换算和异常值剔除。
- 陀螺角速度积分；对 `dt` 使用实测时间戳，不能假定循环永远准时。
- Roll/Pitch 优先互补滤波；Yaw 用陀螺积分配合编码器/磁力计/视觉约束抑制漂移。
- 一维/简化卡尔曼作为可切换模块准备，不允许在六天冲刺中因调不稳阻塞基础闭环。

输出统一为 `roll_deg`、`pitch_deg`、`yaw_deg` 和健康状态。没有磁力计或外部航向观测时，不能宣称 Yaw 长期无漂移。

### 4. 状态机

```text
INIT ──自检通过──> WAIT ──启动──> SEARCH ──发现目标──> TRACK
  │                  │               │                    │
  └──失败──> ERROR   └──急停─────────┴────────────────────┘

TRACK ──目标稳定──> MOVE ──到位──> ACTION ──完成──> FINISH
  │                   │            │
  └──超时──> SEARCH   └──故障──────┴──> ERROR
```

| 状态 | 进入动作 | 循环行为 | 退出条件/保护 |
|---|---|---|---|
| INIT | PWM=0、外设初始化、自检 | 检查电源/编码器/IMU/UART | 全通过进 WAIT；超时进 ERROR |
| WAIT | 清 PID/里程计，保持安全 | 等按键/上位命令 | 启动进 SEARCH；急停进 ERROR |
| SEARCH | 限速搜索 | 巡线/旋转寻找目标 | 目标连续有效 N 帧进 TRACK |
| TRACK | 清视觉控制器瞬态 | 过滤视觉误差并对准 | 稳定窗口满足进 MOVE；丢失超时回 SEARCH |
| MOVE | 设置距离/速度/航向目标 | 双闭环运动，持续监控视觉 | 到位进 ACTION；保护触发进 ERROR |
| ACTION | 停车并执行机构动作 | 计时、限位/电流检测 | 成功进入下一状态/FINISH，超时进 ERROR |
| ERROR | 立即 PWM=0、禁用驱动 | 闪灯/遥测错误码 | 只允许明确复位或人工确认 |
| FINISH | 停车、保存结果 | 保持安全，低频遥测 | 重新开始需回 WAIT 并清状态 |

### 六天任务

| 日期 | 主控任务 | 当日验收标准 |
|---|---|---|
| Day1 7/23 | 修复 `.cproject`、纳入源目录；完成 GPIO/PWM/UART 环回；建立 `app_init/app_run` 和安全停车 | Clean/Rebuild 无错误；`.map` 出现业务符号；示波器确认 PWM；UART 环回 10 min |
| Day2 7/24 | 完成双编码器、1 kHz 调度、PID 库、视觉协议解析和 CRC | 手转计数正确；PID 单元边界测试通过；好/坏/截断/粘包帧均正确处理 |
| Day3 7/25 | 完成双轮速度闭环、速度斜坡、超时停车；采集阶跃曲线 | 左右轮均闭环；三档速度稳态误差达团队阈值；拔视觉串口后 ≤100 ms 降级 |
| Day4 7/26 | 完成位置/航向外环、IMU 基础驱动与互补滤波、状态机 | 定距与 90° 转向各 10 次统计；状态迁移日志无非法跳转 |
| Day5 7/27 | 只做参数整定、异常注入、完整模拟赛 | 完整流程 10 次成功率达到团队门槛；故障均进入安全态 |
| Day6 7/28 | 冻结代码与参数；打 tag/记录哈希；准备回滚固件 | Release 构建可重复；主/备固件均可烧录；2 h 稳定运行 |

## 成员 C：视觉负责人（MaixCAM2）

### 当前基础

- 可复用：OpenCV 四边形检测、中心误差、YOLO11 黑框检测、短时预测、模型文件和 MaixVision 工程描述。
- 必须补齐：UART 二进制协议、巡线、色块、圆/矩形统一接口、真实 FPS/延迟/丢帧/误检数据。
- 禁止：视觉端直接控制电机 PWM。视觉只输出观测、误差、置信度和时间/序号；MSPM0 决定控制量、状态和安全策略。

### 1. 巡线

```text
摄像头图像 → 灰度 → 二值化/自适应阈值 → 下半区多 ROI
→ 每个 ROI 黑线质心 → 加权中心/拟合方向 → error_x、angle、confidence
→ UART → MSPM0 巡线控制器
```

- ROI 至少分近/中/远三段，近端权重大但要防止车头遮挡。
- 输出定义统一为 `error_x = x_target - x_center`；代码当前矩形检测用的是 `center_x - screen_center_x`，接入统一协议时必须明确符号并加方向测试。
- 丢线时 `valid=0`，可携带最后方向提示，但不得伪装成真实观测。

### 2. 色块识别

- 采集比赛灯光下的数据，用 LAB 优先、RGB 辅助建立阈值表。
- 形态学去噪后做连通域，按面积、长宽比和位置筛选最大有效目标。
- 输出目标中心 `X/Y`、面积/尺度、颜色 ID、置信度和 `valid`。
- 阈值存到独立 `vision_config.py`，不散落在主循环。

### 3. 目标识别

| 能力 | 当前状态 | 冲刺交付 |
|---|---|---|
| 矩形/黑框 | 传统 + YOLO11 已有 | 修正文档、统一输出、量化识别距离/角度/遮挡能力 |
| 圆形检测 | 【缺失，需要补充】 | Hough/轮廓圆度二选一，输出圆心和半径 |
| 数字识别 | 【缺失，需要补充】 | 先留模型接口；只有赛题高概率或模型现成时投入 |
| AprilTag | 【缺失，需要补充】 | 确认 MaixPy API 后作为可插拔模式，不阻塞基础功能 |
| 巡线 | 【缺失，需要补充】 | Day2 完成三 ROI 版本 |
| 色块 | 【缺失，需要补充】 | Day3 完成 LAB 阈值版本 |

### 4. 视觉通信协议

统一使用小端定长 16 字节帧，115200 8N1；视觉端建议 30～50 Hz 发送，MSPM0 接收不得依赖 `print()` 文本。

| 偏移 | 字段 | 长度 | 说明 |
|---:|---|---:|---|
| 0 | SOF | 2 | 固定 `0xAA 0x55` |
| 2 | version | 1 | 首版 `0x01` |
| 3 | msg_id | 1 | `0x01`巡线、`0x02`色块、`0x03`矩形、`0x04`圆、`0x05`Tag |
| 4 | seq | 1 | 逐帧递增，用于检测丢帧 |
| 5 | flags | 1 | bit0 valid、bit1 predicted、bit2 calibrated、bit7 error |
| 6 | x | 2 | 有符号：按消息定义为像素误差或中心 X |
| 8 | y | 2 | 有符号：中心 Y/纵向误差 |
| 10 | angle | 2 | 有符号，单位 0.1°；无效填 0 |
| 12 | confidence | 1 | 0～100 |
| 13 | aux | 1 | 颜色 ID/尺度分档/Tag ID 低字节 |
| 14 | crc16 | 2 | CRC16-CCITT，覆盖字节 0～13，小端发送 |

协议规则：`valid=0` 时 MSPM0 不使用坐标闭环；`predicted=1` 时降低增益和速度；帧龄超过 100 ms 视为失联；连续 CRC 错误只计数并重同步，不在 ISR 中打印。MSPM0 可用另一消息 ID 低频回传工作模式和时间戳，便于视觉切换算法。

### 六天任务

| 日期 | 视觉任务 | 当日验收标准 |
|---|---|---|
| Day1 7/23 | 合并 AI 重复代码；修正 README/版本号；实现 `vision_uart.py` 和 16 字节帧 | PC/逻辑分析仪解码 10 万帧，CRC 与字节序正确；无阻塞导致的明显降帧 |
| Day2 7/24 | 三 ROI 巡线、误差/角度/置信度输出；建立明暗两套参数 | 直线、弯线、短时断线视频集有记录；输出符号与 MSPM0 转向一致 |
| Day3 7/25 | LAB 色块 + 连通域；圆形检测；整理 `vision_config.py` | 多颜色各 100 帧统计；错误目标不驱动主控；圆心抖动达团队阈值 |
| Day4 7/26 | 传统/AI 矩形统一输出；透视和相机标定；降低预测风险 | 远近/倾斜/遮挡场景统计；预测帧明确置位；丢失能及时 invalid |
| Day5 7/27 | 随车全流程，调 ROI、曝光、阈值和发帧频率 | 10 次完整模拟赛；保存失败帧并只修高频失败原因 |
| Day6 7/28 | 冻结模型、参数、MaixPy 版本和部署包；备份 SD 卡/工程 | 冷启动 10 次；主备部署包可恢复；运行 2 h 无崩溃或内存增长 |

---

# 第四部分 控制算法库设计

## 1. PID 库：`Algorithm/pid.c/.h`

### 接口与数据结构

```c
typedef struct {
    float kp, ki, kd;
    float integral;
    float prev_error;
    float prev_measurement;
    float out_min, out_max;
    float integral_min, integral_max;
    float deadband;
    float dt_s;
    uint8_t enabled;
} pid_t;

void pid_init(pid_t *pid, float dt_s);
void pid_set_gains(pid_t *pid, float kp, float ki, float kd);
void pid_set_limits(pid_t *pid, float out_min, float out_max,
                    float integral_min, float integral_max);
void pid_reset(pid_t *pid);
float pid_position_update(pid_t *pid, float setpoint, float measurement);
float pid_incremental_update(pid_t *pid, float setpoint, float measurement);
```

必须具备：参数合法性检查、输出限幅、积分限幅、输出饱和时停止同向积分、停车/模式切换复位、固定 `dt` 检查。速度环先用 PI，确认编码器噪声后再决定是否启用 D 项。

## 2. Filter 库：`Algorithm/filter.c/.h`

| 算法 | 用途 | 要求 |
|---|---|---|
| 滑动平均 | ADC、编码器低频观测 | 固定数组，无动态内存；窗口大小编译期限制 |
| 一阶低通 | 轮速、视觉误差 | 参数由截止频率和实测 `dt` 计算 |
| 互补滤波 | IMU Roll/Pitch | 陀螺短期 + 加速度长期；静止与运动分别验收 |
| 卡尔曼滤波 | 可选角度/一维状态估计 | 模块可关闭；先记录噪声方差，不靠盲调 |

## 3. Motion 库：`Algorithm/motion.c/.h`

- 两轮差速运动学：`v_left = v - ω·track/2`，`v_right = v + ω·track/2`。
- 里程计：编码器脉冲 → 左右轮位移 → 车体 `x/y/yaw`；轮径、轮距、PPR 全部来自 `board_config.h`。
- 定距离：位置外环给出线速度，航向外环给出角速度，再分配到左右轮。
- 定角度：原地/小半径旋转两种策略，到位要求角误差和角速度同时满足。
- 轨迹规划：六天内优先梯形/S 曲线速度限制，不追求复杂全局规划。
- 每个动作都提供 `start/update/is_finished/abort`，状态机不得在动作内部无限阻塞。

## 4. 控制周期预算

| 任务 | 建议频率 | 约束 |
|---|---:|---|
| 编码器快照 + 速度环 | 1 kHz | 任务执行时间应显著小于 1 ms；禁止打印 |
| IMU 采样/融合 | 200～500 Hz | I2C 必须有超时；必要时 DMA/状态机化 |
| 位置/航向外环 | 50～100 Hz | 只输出速度/角速度目标 |
| 视觉协议解析 | 收到即入缓冲，100～200 Hz 处理 | ISR 仅搬运字节；解析在主循环 |
| 状态机 | 100 Hz | 所有动作非阻塞 |
| 遥测 | 10～20 Hz | 可降频/关闭，不影响闭环 |

---

# 第五部分 MaixCAM2 可能发挥方向

| 题型能力 | MaixCAM2 负责 | MSPM0 负责 | 最小接口 |
|---|---|---|---|
| 巡线小车 | ROI、线中心、方向、置信度、丢线判断 | 速度规划、巡线 PID、双轮闭环、丢线降速/停车 | `error_x, angle, confidence, valid` |
| 自动寻找目标 | 扫描画面、检测/分类、目标排序 | 控制车体/云台搜索轨迹、状态切换 | `target_id, x, y, confidence` |
| 目标定位停车 | 目标中心、尺度/透视距离提示 | 距离/航向闭环、刹车与到位判定 | `error_x, scale/y, confidence` |
| 颜色分类 | LAB/RGB 分割、连通域、颜色 ID | 决定路线/执行动作、控制机构 | `color_id, x, y, valid` |
| 图形识别 | 圆/矩形/数字/Tag 检测 | 根据类别执行状态机和运动 | `msg_id/type, pose, confidence` |
| 视觉辅助闭环 | 连续测量目标与画面中心误差 | 实时 PID、执行器限位、超时保护 | `ex, ey, seq, flags` |

统一边界：MaixCAM2 是感知节点，不直接控制电机。它可以做图像滤波、特征提取、AI 推理、坐标变换和置信度估计；MSPM0 必须拥有最终状态机、运动闭环、PWM、急停和故障降级。这样视觉程序崩溃、串口断线或模型误检时，小车仍能在 100 ms 级进入可控安全状态。

---

# 第六部分 比赛前 6 天计划

## 总体里程碑

```text
Day1 可构建/可驱动/可通信
  ↓
Day2 编码器速度环 + 视觉巡线协议
  ↓
Day3 位置运动 + 色块/圆形 + 安全保护
  ↓
Day4 状态机完整联调
  ↓
Day5 全流程模拟比赛与故障注入
  ↓
Day6 冻结、备份、连续运行
```

## Day1 — 2026-07-23：消除工程阻断项

- 硬件：完成 BOM/接线/电源树，确认所有型号和极性；抬轮搭好双电机、编码器、MaixCAM2 UART。
- 主控：修复 `MSPM0G35507` 路径；让业务目录进入构建；完成 PWM、DIR/EN、安全停机和 UART 环回。
- 视觉：修正文档与版本号，去除重复真源，完成定长协议发送。
- 联调：MaixCAM2 每帧发送递增 `seq`，MSPM0 校验 CRC 后点灯/回传计数；拔线触发超时。
- 验收：Clean/Rebuild 成功；`.map` 有业务符号；PWM 波形正确；10 万通信帧无死锁；上电和失联均 PWM=0。

## Day2 — 2026-07-24：打通速度闭环与巡线数据链

- 硬件：确认编码器信号完整、方向一致；检查满速波形和驱动温升。
- 主控：完成 Encoder、1 kHz 调度、PID、左右轮速度闭环、斜坡限速。
- 视觉：完成三 ROI 巡线与 `error_x/angle/confidence` 输出。
- 联调：小车架空完成速度阶跃；低速落地直线；人为左右移动线条验证转向符号。
- 验收：编码器人工 10 圈零漏计；三档速度有曲线；巡线误差符号无反；视觉超时 ≤100 ms 降级。

## Day3 — 2026-07-25：形成通用运动与多目标能力

- 硬件：完成 IMU/灰度/电源采样和干扰整改；保存电源纹波截图。
- 主控：完成定距离、定角度、里程计、IMU 互补滤波、故障监控。
- 视觉：完成 LAB 色块、圆形检测、矩形传统/AI 统一输出。
- 联调：实现“巡线 → 发现目标 → 对准 → 停车”的最小演示。
- 验收：定距和 90° 转向各 10 次统计；每种视觉模式有百帧数据；电机阶跃不复位。

## Day4 — 2026-07-26：状态机与完整流程

- 硬件：机械与线束定型，相机/IMU 安装方向固化。
- 主控：完成 INIT/WAIT/SEARCH/TRACK/MOVE/ACTION/ERROR/FINISH；所有动作非阻塞。
- 视觉：完成相机标定、模式切换、曝光/阈值配置和预测帧标记。
- 联调：按预设模拟题跑完整流程，注入丢目标、坏 CRC、堵转/FAULT、传感器断线。
- 验收：不存在非法状态跳转；所有 P0 故障进入 ERROR 并停车；完整流程连续成功 5 次。

## Day5 — 2026-07-27：模拟比赛

- 硬件：按正式布置、正式电池、正式场地连续运行；演练关键备件更换。
- 主控：只调参数和修 P0/P1，不再重写架构；记录每次运行参数版本。
- 视觉：采集强光、暗光、倾斜、遮挡、运动模糊失败样本，只修复最高频问题。
- 联调：计时封闭模拟赛至少 10 轮，成员按比赛职责操作，不边跑边改代码。
- 验收：成功率达到团队设定门槛；失败均有日志、可复现、归因到硬件/主控/视觉之一；15 min 内能换备件恢复。

## Day6 — 2026-07-28：冻结版本

- 硬件：贴版本/端口标签，整理主车、备件、工具、线材、充电器和文档。
- 主控：生成 Release 固件、`.map`、参数表、Git tag；准备稳定版与保守降速版。
- 视觉：冻结 MaixPy 版本、模型、参数和 MaixVision 工程；制作可恢复备份。
- 联调：连续运行 2 h；断电恢复 10 次；冷启动、低电、通信中断、目标丢失复测。
- 验收：零复位、零失控、零不可恢复死机；主备固件/视觉包可从新目录重新部署；已知问题清单写明规避方式。

## 7 月 29 日比赛日变更规则

- 只允许改 `Application/app_task_*`、`app_config.h`、`control_params.h` 和 `vision_config.py` 中的赛题适配参数。
- Driver/BSP/协议/PID 基础库默认冻结；除非出现阻断性硬件不兼容，否则不改底层。
- 每次改动先复制稳定版本、记录 Git commit/hash；至少保留“昨晚稳定版”和“当前比赛版”两套可烧录包。

---

# 第七部分 最终验收清单

## A. 硬件

- [ ] 电源树、保险、急停和连接器均有标签
- [ ] 3.3 V/5 V/电机母线稳定，纹波和启动跌落有示波器记录
- [ ] 电机正反转、制动、使能、FAULT 均正常
- [ ] 电机启动/反转/堵转保护过程中 MCU 不复位
- [ ] 双编码器方向、计数和 10 圈校验正常
- [ ] MaixCAM2 独立供电稳定，与 MSPM0 共地且 UART 为 3.3 V
- [ ] IMU 方向、零偏和固定方式确认
- [ ] 电磁铁/继电器/舵机动作 100 次无 MCU/视觉异常
- [ ] 强弱电走线和功率回流路径检查完成
- [ ] 关键模块有备件，15 min 内可替换恢复

## B. 主控

- [ ] Clean/Rebuild 无错误，业务源文件确实进入 `.map`
- [ ] GPIO/UART/I2C/SPI/PWM/ADC/DMA/Timer/Encoder 按实际硬件完成
- [ ] UART 具备环形缓冲、CRC、乱流重同步和错误计数
- [ ] 视觉帧超时 ≤100 ms 时降级/停车
- [ ] 左右轮速度 PID 完成并有阶跃曲线
- [ ] 位置—速度双闭环完成
- [ ] 定距离和定角度各完成 10 次统计
- [ ] IMU 零偏、滤波和姿态输出完成；Yaw 漂移边界写明
- [ ] INIT/WAIT/SEARCH/TRACK/MOVE/ACTION/ERROR/FINISH 状态机完成
- [ ] 看门狗、欠压、驱动 FAULT、传感器超时均能进入安全态
- [ ] ISR 内无阻塞、打印或复杂算法；共享数据保护正确
- [ ] 无无边界动态内存；Flash/RAM 使用量有 `.map` 记录

## C. 视觉

- [ ] 巡线：三 ROI、中心误差、角度、置信度、丢线状态
- [ ] 色块：LAB/RGB 参数、连通域、最大目标、颜色 ID
- [ ] 矩形：传统与 AI 输出统一，README 与版本号一致
- [ ] 圆形检测可切换；数字/AprilTag 至少有明确接口或放弃说明
- [ ] 二进制 UART 协议、CRC、`seq`、`valid/predicted` 标志完成
- [ ] 视觉不直接输出 PWM、不直接控制电机
- [ ] 强光/暗光/倾斜/遮挡/运动模糊测试记录完成
- [ ] 模型、参数、MaixPy 版本和部署步骤全部冻结

## D. 系统

- [ ] 上电默认停车，自检失败进入 ERROR
- [ ] 完整比赛流程连续成功至少 10 次
- [ ] 连续运行 2 h 无复位、失控、内存异常或通信锁死
- [ ] 断电恢复 10 次正常
- [ ] 视觉断线、坏 CRC、目标丢失、编码器异常均有保护
- [ ] 低电压、驱动故障和急停均能切断运动输出
- [ ] 稳定版/保守版固件与视觉工程均可独立恢复
- [ ] Git tag、commit hash、参数版本、接线图、BOM 和已知问题清单齐全

---

# 最终执行优先级

## P0：不完成就不能参赛

1. 修复构建路径并让业务目录实际参与编译。
2. 确认电机驱动型号/接线，完成安全 PWM、方向和使能。
3. 双编码器 + 双轮速度闭环。
4. MaixCAM2 二进制 UART + MSPM0 CRC/超时解析。
5. 状态机、急停、失联停车、看门狗。

## P1：形成控制类通用工程

1. 位置/航向外环、差速运动学、定距/定角。
2. 巡线、色块、矩形/圆形统一视觉接口。
3. IMU 基础姿态与传感器健康管理。
4. 参数配置、日志、标定和完整模拟赛。

## P2：有余力再做

1. 卡尔曼精调、复杂轨迹规划。
2. 数字识别、AprilTag、多模型自动切换。
3. 更复杂的视觉预测；预测永远不能替代安全有效观测。

完成标准不是“目录和函数都建了”，而是：能重复构建、能在实物上测量、能在异常下安全停车、能由另一名队员按文档在 15 分钟内复现。达到这个标准后，比赛换题才真正只需要改 Application 层和少量参数。
